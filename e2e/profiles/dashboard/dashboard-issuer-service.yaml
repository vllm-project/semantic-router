apiVersion: v1
kind: Service
metadata:
  name: semantic-router-dashboard-issuer
  labels:
    app: semantic-router-dashboard
    vllm.ai/e2e-managed: "true"
spec:
  type: ClusterIP
  selector:
    app: semantic-router-dashboard
  ports:
  - name: https-issuer
    port: 9443
    targetPort: https-issuer
