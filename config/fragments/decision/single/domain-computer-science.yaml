document:
  decisions:
    - name: computer-science-route
      description: Route computer science requests to the higher-reasoning model tier.
      priority: 100
      rules:
        operator: AND
        conditions:
          - type: domain
            name: "computer science"
