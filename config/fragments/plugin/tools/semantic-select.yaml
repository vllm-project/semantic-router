plugin:
  type: tools
  configuration:
    enabled: true
    mode: filtered
    semantic_selection: true
    allow_tools:
      - docs.search
      - tickets.lookup
    block_tools:
      - admin.delete
    # Privacy routes should instead use mode: none together with
    # strip_tool_history: true so prior calls/results are not provider-bound.
