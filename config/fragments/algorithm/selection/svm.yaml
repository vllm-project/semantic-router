algorithm:
  type: svm
  ml:
    models_path: /models/selection
    model_type: qwen3
    embedding_dim: 1024
    svm:
      kernel: rbf
      gamma: 1.0
      pretrained_path: /models/selection/svm_model.json
