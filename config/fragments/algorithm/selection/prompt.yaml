document:
  decisions:
    - name: prompt-selected-route
      description: Select from the Models assigned by the Entrypoint.
      priority: 100
      rules:
        operator: AND
        conditions: []
      algorithm:
        type: prompt
        prompt:
          instructions: >-
            Choose the most suitable assigned candidate for the request.
          timeout_seconds: 5
