routing:
  signals:
    metadata:
      - name: consent-denied
        description: Caller explicitly denied remote processing consent.
        key: consent
        predicate:
          equals: denied
