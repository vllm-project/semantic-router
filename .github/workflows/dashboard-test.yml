name: Integration Test [Dashboard]

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual triggering

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Use changes filter to detect dashboard changes
  changes:
    uses: ./.github/workflows/ci-changes.yml

  dashboard-test:
    needs: changes
    if: >-
      ${{ !github.event.pull_request.draft
          && needs.changes.outputs.dashboard == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    name: Dashboard Build and Test

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Node.js dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            dashboard/frontend/node_modules
          key: ${{ runner.os }}-node-dashboard-${{ hashFiles('dashboard/frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-dashboard-

      - name: Cache Go dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-dashboard-${{ hashFiles('dashboard/backend/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-dashboard-

      - name: Set up golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.5.0
          install-mode: binary
          args: --help

      - name: Install dashboard runtime test dependencies
        run: python -m pip install -e ./src/vllm-sr

      - name: Run dashboard checks
        run: make dashboard-check

      - name: Audit deployed frontend dependencies
        working-directory: dashboard/frontend
        run: npm audit --omit=dev --audit-level=moderate

      - name: Run dashboard backend race tests
        run: make dashboard-test-backend-race

      - name: Install Playwright Chromium
        working-directory: dashboard/frontend
        run: npx playwright install --with-deps chromium

      - name: Run focused dashboard authentication browser tests
        run: make dashboard-test-auth-e2e

      - name: Build dashboard
        run: make dashboard-build

      - name: Show results on failure
        if: failure()
        run: |
          echo "::error::Dashboard checks failed. Please fix the issues and commit again."
          echo ""
          echo "To run checks locally:"
          echo "  make dashboard-check"
          echo "  make dashboard-test-backend-race"
          echo "  make dashboard-test-auth-e2e"
          echo ""
          echo "To auto-fix lint issues:"
          echo "  make dashboard-lint-fix"
