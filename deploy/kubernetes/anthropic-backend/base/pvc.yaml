apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anthropic-backend-models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # GGUF model (~400 MB) plus headroom
