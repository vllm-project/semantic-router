# Production profile — hardened defaults for multi-replica, production clusters.

replicaCount: 2

image:
  pullPolicy: IfNotPresent

resources:
  limits:
    memory: "8Gi"
    cpu: "4"
  requests:
    memory: "4Gi"
    cpu: "2"

persistence:
  enabled: true
  size: 20Gi

dashboard:
  enabled: true
  replicaCount: 1
  persistence:
    enabled: true
    size: 5Gi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

podDisruptionBudget:
  enabled: true
  minAvailable: 1

topologySpread:
  enabled: true
  maxSkew: 1
  topologyKey: topology.kubernetes.io/zone
  whenUnsatisfiable: ScheduleAnyway

startupProbe:
  failureThreshold: 360  # 60 minutes

livenessProbe:
  initialDelaySeconds: 30
  periodSeconds: 30
  failureThreshold: 5

readinessProbe:
  initialDelaySeconds: 30
  periodSeconds: 15
  failureThreshold: 3

securityContext:
  runAsNonRoot: true
  allowPrivilegeEscalation: false

dependencies:
  observability:
    jaeger:
      enabled: false
    prometheus:
      enabled: true
    grafana:
      enabled: true
