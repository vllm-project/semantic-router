// Dashboard and website intentionally bundle the same generated public
// snapshot. Runtime API data can replace this fallback after page load, but a
// second checked-in JSON mirror must never become another catalog inventory.
import modelCatalogDocument from '../../../website/static/model-catalog/catalog.json'

export default modelCatalogDocument
