# Install the matching CPU/ROCm PyTorch 2.13 wheel before these requirements.
# Transformers 5 uses per-attention-type ModernBERT RoPE parameters; the
# trainer validates both persisted configuration and instantiated frequencies.
# CC-100 HTTP/XZ streaming uses the Python standard library, so no unpinned
# downloader is introduced here.
# Compatibility pin only: GHSA-4j2p-28q2-5m79 is still unpatched; see README.
accelerate==1.12.0
datasets==4.5.0
huggingface-hub==1.5.0
langdetect==1.0.9
numpy==2.2.2
safetensors==0.5.2
scikit-learn==1.6.1
sentence-transformers==5.3.0
tqdm==4.67.1
transformers==5.10.1
torch==2.13.0
