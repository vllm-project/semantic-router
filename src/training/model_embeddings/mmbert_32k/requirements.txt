# PyTorch is supplied by the digest-pinned ROCm base image in runtime.json.
# transformers==4.57.6 is part of the foundation YaRN contract: the trainer
# uses its config-driven ModernBERT rotary implementation and validates it.
# CC-100 HTTP/XZ streaming uses the Python standard library, so no unpinned
# downloader is introduced here.
accelerate==1.12.0
datasets==4.5.0
huggingface-hub==0.36.0
langdetect==1.0.9
numpy==2.2.2
safetensors==0.5.2
scikit-learn==1.6.1
sentence-transformers==5.3.0
tqdm==4.67.1
transformers==4.57.6
