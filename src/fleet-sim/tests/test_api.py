"""Compatibility anchor for the split fleet-sim API test suite.

The route, runner-contract, and job-lifecycle coverage now lives in
adjacent `test_api_*.py` modules so the legacy API suite no longer needs a
single oversized test file.
"""
