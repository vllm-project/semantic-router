// Package managementapi defines the Router Management API v1 transport
// contract. It owns public wire types, operation authorization metadata, and
// deterministic OpenAPI generation; it deliberately owns no HTTP server,
// handler, persistence, or access-control business logic.
package managementapi
